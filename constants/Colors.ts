/**
 * @deprecated Import `colors` from `@/constants/designTokens` for new code.
 * This compatibility export keeps existing call sites working while the
 * visual system is migrated incrementally.
 */
export { colors as default } from './designTokens';
